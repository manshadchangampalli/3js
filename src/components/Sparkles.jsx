import { Sparkles } from "@react-three/drei";

const SparklesComponent = () => {
    return (
        <Sparkles count={1000} speed={0.2} scale={5} size={2} color={'#00ffff'} />
    );
};

export default SparklesComponent